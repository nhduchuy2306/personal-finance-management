spring:
  application:
    name: budget-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:http://${CONFIG_SERVER_HOST:localhost}:9888
  cloud:
    config:
      name: ${spring.application.name},db-config,cache,kafka,discovery
      fail-fast: false
