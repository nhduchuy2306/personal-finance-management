spring:
  application:
    name: auth-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      name: ${spring.application.name},db-config,cache,kafka,discovery,logging
      fail-fast: false
